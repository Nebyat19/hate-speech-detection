# Build-time branding for the Vite client (restart dev server after changes).
VITE_APP_NAME=safetext
VITE_APP_TAGLINE=Community safety & moderation

# Dev proxy target for the Vite client. Override when the backend is not on localhost.
VITE_API_PROXY_TARGET=http://localhost:3001
